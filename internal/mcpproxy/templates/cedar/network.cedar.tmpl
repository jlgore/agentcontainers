{{- /* sift.network — informational only (egress is not observable from tool arguments; the eBPF layer enforces it). No forbid policy is emitted, mirroring the Rego network package which never denies a tools/call. */ -}}
// sift.network: informational — egress enforced at the kernel (eBPF), not here.
